<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.baixiaosheng.inventory">
    <!-- 基础权限 -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- 存储权限适配（核心修正） -->
    <!-- 1. Android 13+ 照片权限（替代READ_EXTERNAL_STORAGE） -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- 2. Android 6-12 读取外部存储权限 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- 3. Android 11+ 全文件访问权限（注意：这个权限需要特殊申请） -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- 4. Android 9及以下写入权限 -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:node="replace"/>

    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- 声明应用使用的功能 -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false"/>
    <uses-feature
        android:name="android.hardware.camera.autofocus"
        android:required="false"/>

    <!-- Android 11+ 包可见性配置 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="http" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.intent.action.GET_CONTENT" />
            <data android:mimeType="image/*" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Inventory"
        android:requestLegacyExternalStorage="true"> <!-- 适配Android 10文件访问 -->

        <!-- 主Activity -->
        <activity
            android:name=".view.activity.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".view.activity.RecycleActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.AboutActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.CategoryManageActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.DataManageActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.LocationManageActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.ItemDetailActivity"
            android:exported="false" />
        <activity
            android:name=".view.activity.InputActivity"
            android:exported="false" />


        <!-- FileProvider配置 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.baixiaosheng.inventory.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"/>
        </provider>

    </application>

</manifest>